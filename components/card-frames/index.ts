import HoloFrame from './HoloFrame';

// To swap the card design: change ActiveFrame to any other frame component.
// The frame component must accept the same props as PlayerCard.
export const ActiveFrame = HoloFrame;
